# Python toolchain for this repo — installed into the repo-local .venv by
# scripts/ensure-venv.sh (nothing is installed system-wide). Renovate bumps
# these pins, and CI proves the bump before it merges.
ansible-core==2.21.2
ansible-lint==26.6.0
